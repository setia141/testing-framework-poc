[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mutation-test-sample"
version = "1.0.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
